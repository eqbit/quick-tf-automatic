export type TConfigFields = {
  bptfAccessToken: string;
  bptfApiKey: string;
  telegramApiToken: string;
  telegramNotificationChannel: string;
  steamid: string;
  steamguard: string;
  oAuthToken: string;
  identity_secret: string;
};
